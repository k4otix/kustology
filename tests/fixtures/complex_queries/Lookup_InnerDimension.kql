DeviceLogonEvents
| where ActionType == "LogonSuccess"
| lookup kind=inner (DeviceInfo | project DeviceId, DeviceName, OSPlatform) on DeviceId
| project Timestamp, DeviceName, OSPlatform, AccountName
